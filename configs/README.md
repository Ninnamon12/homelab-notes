# Configs

Sanitized copies of what ENDOR actually runs. Not a compose museum.

Public side of Ninnamon12/homelab-notes. Never commit host paths, LAN addresses, GPU UUIDs, or real passwords.

Layout: configs/<app>/ with README plus Quadlets or host units. Wolf is rootful. Immich is rootless. P100 fan control is a host systemd service, not a container.
